name: Auto Update Dashboard

on:
  schedule:
    - cron: '*/30 * * * *'  # Every 30 minutes
  workflow_dispatch:  # Manual trigger from admin page
    inputs:
      force_run:
        description: 'Force full pipeline run (skip change detection)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # ========== Step 1: Checkout ==========
      - name: "Step 1: Checkout repository"
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ========== Step 2: Setup Python ==========
      - name: "Step 2: Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: "Step 2.1: Install dependencies"
        run: |
          pip install -r requirements.txt

      # ========== Step 3: Download from Google Drive ==========
      - name: "Step 3: Download data from Google Drive"
        env:
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
        run: |
          echo "📥 Downloading latest data from Google Drive..."
          python scripts/download_from_gdrive.py
          echo "✅ Google Drive download complete"

      # ========== Step 4: Sync Thresholds from Firestore ==========
      - name: "Step 4: Sync thresholds from Firestore"
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          echo "🔄 Syncing thresholds from Firestore..."
          # Determine current month/year
          CURRENT_MONTH=$(python3 -c "
          import datetime
          months = ['january','february','march','april','may','june','july','august','september','october','november','december']
          print(months[datetime.datetime.now().month - 1])
          ")
          CURRENT_YEAR=$(date +%Y)
          echo "Current period: $CURRENT_MONTH $CURRENT_YEAR"
          python scripts/sync_thresholds.py --month "$CURRENT_MONTH" --year "$CURRENT_YEAR" || echo "⚠️ Threshold sync failed, using local config"
          echo "✅ Threshold sync complete"

      # ========== Step 4.5: Sync Config Settings from Firestore ==========
      - name: "Step 4.5: Sync config settings from Firestore"
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          echo "🔄 Syncing config settings from Firestore..."
          python scripts/sync_configs.py || echo "⚠️ Config sync failed, using local config files"
          echo "✅ Config sync complete"

      # ========== Step 5: Enhanced Download with Config ==========
      - name: "Step 5: Enhanced download with config update"
        env:
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
        run: |
          echo "📋 Running enhanced download with config..."
          python scripts/enhanced_download.py || echo "⚠️ Enhanced download skipped"
          echo "✅ Config update complete"

      # ========== Step 5.5: Determine Target Month ==========
      - name: "Step 5.5: Determine target month from config files"
        id: target_month
        run: |
          echo "🎯 Determining target month..."
          # Find the latest month config that has actual data (file_paths defined)
          LATEST_CONFIG=$(python3 -c "
          import glob, re, json
          months = {'january':1,'february':2,'march':3,'april':4,'may':5,'june':6,
                    'july':7,'august':8,'september':9,'october':10,'november':11,'december':12}
          configs = glob.glob('config_files/config_*_*.json')
          best = None
          best_key = (0, 0)
          for f in configs:
              m = re.search(r'config_(\w+)_(\d{4})\.json', f)
              if m:
                  month_name, year_str = m.group(1), m.group(2)
                  month_num = months.get(month_name, 0)
                  year = int(year_str)
                  # Only consider configs with actual downloaded data (file_paths)
                  try:
                      with open(f) as fh:
                          cfg = json.load(fh)
                      if not cfg.get('file_paths'):
                          continue  # Skip threshold-only configs
                  except:
                      continue
                  if (year, month_num) > best_key:
                      best_key = (year, month_num)
                      best = f
          print(best or '')
          ")
          echo "Latest config: $LATEST_CONFIG"
          if [ -n "$LATEST_CONFIG" ]; then
            MONTH=$(python3 -c "import json; c=json.load(open('$LATEST_CONFIG')); print(c.get('month', ''))")
            YEAR=$(python3 -c "import json; c=json.load(open('$LATEST_CONFIG')); print(c.get('year', ''))")
            echo "target_config=$LATEST_CONFIG" >> $GITHUB_OUTPUT
            echo "target_month=$MONTH" >> $GITHUB_OUTPUT
            echo "target_year=$YEAR" >> $GITHUB_OUTPUT
            echo "🎯 Target: $MONTH $YEAR ($LATEST_CONFIG)"
          else
            echo "❌ No config file found!"
            exit 1
          fi

      # ========== Step 5.6: Change Detection ==========
      - name: "Step 5.6: Check for data changes"
        id: change_check
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FORCE_RUN: ${{ github.event.inputs.force_run || 'false' }}
        run: |
          if [ "$FORCE_RUN" = "true" ]; then
            echo "🔧 Force run requested - skipping change detection"
            echo "data_changed=true" >> $GITHUB_OUTPUT
          else
            echo "🔍 Checking for data changes..."
            python scripts/check_drive_changes.py check
            if [ $? -eq 0 ]; then
              echo "🔄 Changes detected - continuing pipeline"
              echo "data_changed=true" >> $GITHUB_OUTPUT
            else
              echo "✅ No changes - skipping calculation and upload"
              echo "data_changed=false" >> $GITHUB_OUTPUT
            fi
          fi

      # ========== Step 6: Convert Attendance Data ==========
      - name: "Step 6: Convert attendance data"
        if: steps.change_check.outputs.data_changed == 'true'
        env:
          TARGET_MONTH: ${{ steps.target_month.outputs.target_month }}
          TARGET_YEAR: ${{ steps.target_month.outputs.target_year }}
        run: |
          echo "📊 Converting attendance data for $TARGET_MONTH $TARGET_YEAR..."
          python src/convert_attendance_data.py "$TARGET_MONTH" "$TARGET_YEAR"

          # Also convert previous month's attendance (needed for incentive calculation)
          PREV_MONTH=$(python3 -c "
          months = ['january','february','march','april','may','june','july','august','september','october','november','december']
          idx = months.index('$TARGET_MONTH')
          prev_idx = (idx - 1) % 12
          print(months[prev_idx])
          ")
          PREV_YEAR=$TARGET_YEAR
          if [ "$PREV_MONTH" = "december" ]; then
            PREV_YEAR=$((TARGET_YEAR - 1))
          fi
          echo "📊 Converting previous month attendance: $PREV_MONTH $PREV_YEAR..."
          python src/convert_attendance_data.py "$PREV_MONTH" "$PREV_YEAR" || echo "⚠️ Previous month attendance conversion skipped (data may not exist)"
          echo "✅ Attendance conversion complete"

      # ========== Step 6.5: Validate Schema ==========
      - name: "Step 6.5: Validate attendance schema"
        if: steps.change_check.outputs.data_changed == 'true'
        env:
          TARGET_MONTH: ${{ steps.target_month.outputs.target_month }}
          TARGET_YEAR: ${{ steps.target_month.outputs.target_year }}
        run: |
          echo "🔍 Validating attendance schema for $TARGET_MONTH $TARGET_YEAR..."
          if [ -f "scripts/validate_attendance_schema.py" ]; then
            python scripts/validate_attendance_schema.py "$TARGET_MONTH" "$TARGET_YEAR" || {
              echo "❌ Schema validation failed! Stopping pipeline."
              exit 1
            }
          else
            echo "⚠️ Schema validation script not found, skipping"
          fi
          echo "✅ Schema validation passed"

      # ========== Step 6.8: Download Previous Incentive from Firestore ==========
      - name: "Step 6.8: Download Previous Incentive from Firestore"
        if: steps.change_check.outputs.data_changed == 'true'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          TARGET_MONTH: ${{ steps.target_month.outputs.target_month }}
          TARGET_YEAR: ${{ steps.target_month.outputs.target_year }}
        run: |
          echo "📥 Downloading previous month incentive from Firestore..."
          mkdir -p output_files
          python scripts/download_previous_incentive.py --month "$TARGET_MONTH" --year "$TARGET_YEAR"
          echo "✅ Previous incentive download complete"

      # ========== Step 7: Calculate Incentives ==========
      - name: "Step 7: Calculate incentives"
        if: steps.change_check.outputs.data_changed == 'true'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          TARGET_CONFIG: ${{ steps.target_month.outputs.target_config }}
        run: |
          echo "🧮 Calculating incentives..."
          echo "Using config: $TARGET_CONFIG"
          mkdir -p output_files
          python src/step1_인센티브_계산.py --config "$TARGET_CONFIG"
          echo "✅ Incentive calculation complete"

      # ========== Step 7.5: Update AQL Inspector Config ==========
      - name: "Step 7.5: Update AQL Inspector config"
        if: steps.change_check.outputs.data_changed == 'true'
        env:
          TARGET_MONTH: ${{ steps.target_month.outputs.target_month }}
          TARGET_YEAR: ${{ steps.target_month.outputs.target_year }}
        run: |
          echo "🔄 Updating AQL Inspector config..."
          if [ -f "scripts/auto_update_aql_config.py" ]; then
            python scripts/auto_update_aql_config.py "$TARGET_MONTH" "$TARGET_YEAR" || echo "⚠️ AQL config update skipped"
          fi
          echo "✅ AQL config update complete"

      # ========== Step 8: Upload to Firestore (CORE!) ==========
      - name: "Step 8: Upload results to Firestore"
        if: steps.change_check.outputs.data_changed == 'true'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          TARGET_MONTH: ${{ steps.target_month.outputs.target_month }}
          TARGET_YEAR: ${{ steps.target_month.outputs.target_year }}
        run: |
          echo "🔥 Uploading results for $TARGET_MONTH $TARGET_YEAR to Firestore..."
          python scripts/upload_to_firestore.py --month "$TARGET_MONTH" --year "$TARGET_YEAR"
          echo "✅ Firestore upload complete"

      # ========== Step 8.5: Save Input Hash ==========
      - name: "Step 8.5: Save input hash for change detection"
        if: steps.change_check.outputs.data_changed == 'true'
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          echo "💾 Saving input hash..."
          python scripts/check_drive_changes.py save
          echo "✅ Input hash saved"

      # ========== Step 9: Update System Status ==========
      - name: "Step 9: Update system status in Firestore"
        if: always()
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          DATA_CHANGED: ${{ steps.change_check.outputs.data_changed }}
        run: |
          echo "📊 Updating system status..."
          python3 -c "
          import os, json, datetime
          import firebase_admin
          from firebase_admin import credentials, firestore

          sa_json = os.environ.get('FIREBASE_SERVICE_ACCOUNT', '')
          data_changed = os.environ.get('DATA_CHANGED', 'true')

          if sa_json:
              sa_dict = json.loads(sa_json)
              cred = credentials.Certificate(sa_dict)
              if not firebase_admin._apps:
                  firebase_admin.initialize_app(cred, {'projectId': 'hwk-qip-incentive-dashboard'})
              db = firestore.client()

              status_data = {
                  'last_run_at': firestore.SERVER_TIMESTAMP,
                  'status': 'success',
                  'run_type': 'github_actions',
                  'commit': os.environ.get('GITHUB_SHA', 'unknown')[:8],
                  'data_changed': data_changed == 'true'
              }

              if data_changed == 'true':
                  status_data['last_data_update'] = firestore.SERVER_TIMESTAMP

              db.collection('system').document('status').set(status_data, merge=True)
              print('✅ System status updated (data_changed=' + data_changed + ')')
          else:
              print('⚠️ FIREBASE_SERVICE_ACCOUNT not set, skipping status update')
          "

      # ========== Step 10: Generate Selector Page ==========
      - name: "Step 10: Generate selector page"
        if: steps.change_check.outputs.data_changed == 'true'
        run: |
          echo "📄 Generating selector page..."
          if [ -f "scripts/create_month_selector.py" ]; then
            python scripts/create_month_selector.py || echo "⚠️ Selector generation skipped"
          fi
          echo "✅ Selector generation complete"

      # ========== Step 11: Deploy to GitHub Pages ==========
      - name: "Step 11: Commit and deploy"
        if: steps.change_check.outputs.data_changed == 'true'
        run: |
          echo "🚀 Deploying to GitHub Pages..."

          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Stash any local changes, sync with remote
          git stash push -m "auto-update-stash" 2>/dev/null || true
          git fetch origin main
          git reset --hard origin/main
          git stash pop 2>/dev/null || true

          # Stage web files and configs (NOT output_files or input_files)
          git add web/ config_files/ scripts/ src/ .github/ || true

          # Check if there are changes to commit
          if git diff --cached --quiet; then
            echo "ℹ️ No changes to commit"
          else
            TIMESTAMP=$(TZ='Asia/Ho_Chi_Minh' date '+%Y-%m-%d %H:%M:%S')
            git commit -m "🔄 Auto update dashboard + Firestore sync $TIMESTAMP"

            # Push with retry (max 3 attempts)
            for i in 1 2 3; do
              if git push origin main; then
                echo "✅ Push successful (attempt $i)"
                break
              else
                echo "⚠️ Push failed (attempt $i), retrying..."
                git fetch origin main
                git rebase origin/main --strategy-option=theirs || {
                  git rebase --abort 2>/dev/null
                  git reset --hard origin/main
                  git stash pop 2>/dev/null || true
                  git add web/ config_files/ scripts/ src/ .github/ || true
                  git commit -m "🔄 Auto update dashboard $TIMESTAMP (retry $i)" || true
                }
                sleep 5
              fi
            done
          fi
          echo "✅ Deployment complete"

      # ========== Failure Notification ==========
      - name: "Notify on failure"
        if: failure()
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          echo "❌ Pipeline failed! Updating status..."
          python3 -c "
          import os, json
          import firebase_admin
          from firebase_admin import credentials, firestore

          sa_json = os.environ.get('FIREBASE_SERVICE_ACCOUNT', '')
          if sa_json:
              sa_dict = json.loads(sa_json)
              cred = credentials.Certificate(sa_dict)
              if not firebase_admin._apps:
                  firebase_admin.initialize_app(cred, {'projectId': 'hwk-qip-incentive-dashboard'})
              db = firestore.client()
              db.collection('system').document('status').set({
                  'last_run_at': firestore.SERVER_TIMESTAMP,
                  'status': 'failure',
                  'run_type': 'github_actions',
                  'commit': os.environ.get('GITHUB_SHA', 'unknown')[:8],
                  'error': 'Pipeline failed - check GitHub Actions logs'
              }, merge=True)
              print('⚠️ Failure status recorded in Firestore')
          " || true
